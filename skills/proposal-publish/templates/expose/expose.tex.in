% ============================================================
%  EXPOSÉ — generated by the proposal-publish skill
%  Faculty of Computer Science
% ============================================================
%
%  Generated from: {{SOURCE_FILE}}
%  Do not edit this file by hand — edit the source markdown and
%  run the publish skill again, or detach it from the workflow
%  and take ownership of the .tex from here on.
%
%  Compile with:  pdflatex -> bibtex -> pdflatex -> pdflatex
%  Overleaf runs that sequence automatically.
% ============================================================

% ─────────────────────────────────────────────────────────
%  PREAMBLE
% ─────────────────────────────────────────────────────────
\documentclass[
    a4paper,
    11pt,
    DIV=11,
    bigheadings,
    idxtotoc,
    listof=totoc,
    bibtotoc,
    halfparskip,
    cleardoubleempty,
    openright,
    oneside
]{scrartcl}

% ── Language & encoding ──────────────────────────────────
\usepackage[{{BABEL_LANGS}}]{babel}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ── Layout ───────────────────────────────────────────────
\usepackage{geometry}
\geometry{a4paper, left=30mm, right=20mm}
\usepackage{setspace}
\usepackage{layout}
\setlength{\headsep}{10mm}
\setlength{\footskip}{15mm}
\setlength{\textheight}{235mm}

% ── Graphics ─────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage{wrapfig}

% ── Tables ───────────────────────────────────────────────
\usepackage{longtable}
\usepackage{multirow}
\usepackage{colortbl}
\usepackage{booktabs}

% ── Colour palette ───────────────────────────────────────
\usepackage[dvipsnames]{xcolor}
\usepackage{color}
\definecolor{haw_mag}{rgb}{0,0.112,0.47}        % THI blue – headings
\definecolor{barblue}{HTML}{3B82F6}
\definecolor{bargreen}{HTML}{22C55E}
\definecolor{barorange}{HTML}{F59E0B}
\definecolor{barpurple}{HTML}{8B5CF6}
\definecolor{barred}{HTML}{EF4444}
\definecolor{barteal}{HTML}{14B8A6}
\definecolor{linkgray}{HTML}{64748B}
\definecolor{gridcolor}{HTML}{E2E8F0}

% ── Heading colours ──────────────────────────────────────
\addtokomafont{section}{\color{haw_mag}\rmfamily}
\addtokomafont{subsection}{\color{haw_mag}\rmfamily}
\addtokomafont{subsubsection}{\color{haw_mag}\rmfamily}
\addtokomafont{paragraph}{\color{haw_mag}\rmfamily}
\addtokomafont{subparagraph}{\rmfamily}

% ── Hyperlinks ───────────────────────────────────────────
\PassOptionsToPackage{hyphens}{url}
\usepackage[
    pdfborder={0 0 0},
    colorlinks=true,
    linkcolor=black,
    citecolor=red
]{hyperref}

% ── Bibliography ─────────────────────────────────────────
\usepackage[numbers]{natbib}

% ── Gantt chart ──────────────────────────────────────────
\usepackage{pgfgantt}

% ── Glossary (empty when the source declares no abbreviations) ──
{{GLOSSARY_PREAMBLE}}

% ── Header / footer ──────────────────────────────────────
\usepackage{scrlayer-scrpage}
\pagestyle{scrheadings}
\clearscrheadfoot
\ohead{\pagemark}
\ihead{\ifthispageodd{\leftmark}{\rightmark}}
\setheadsepline{0.4pt}
\setkomafont{pagehead}{\normalfont}
\setfootsepline{0.4pt}
\setkomafont{pagefoot}{\normalfont}
\ifoot{\footnotesize{{{AUTHOR}}}}
\ofoot{\footnotesize{Exposé}}

% ── Custom page style (used on front-matter pages) ───────
\defpagestyle{myPageStyle}{
    (0pt,0pt)
    {\hfill\pagemark}{\hfill\pagemark}{\hfill\pagemark}
    (0pt,0pt)
}{
    {(\textwidth,0.4pt)
    \footnotesize{{{AUTHOR}}}\hfill\footnotesize{Exposé}}
    {\footnotesize{{{AUTHOR}}}\hfill\footnotesize{Exposé}}
    {\footnotesize{{{AUTHOR}}}\hfill\footnotesize{Exposé}}
    (0pt,0pt)
}

% ─────────────────────────────────────────────────────────
%  DOCUMENT BEGIN
% ─────────────────────────────────────────────────────────
\begin{document}

\shorthandoff{"}
\widowpenalty=10000
\clubpenalty=10000
\displaywidowpenalty=10000

% ══════════════════════════════════════════════════════════
%  TITLE PAGE
% ══════════════════════════════════════════════════════════
\newgeometry{textheight=0.9\paperheight, textwidth=0.76\paperwidth,
             left=30mm, right=20mm}

\begin{titlepage}

    \begin{figure}[!h]
        \centering
        \includegraphics[width=0.4\textwidth]{images/thiRGB.jpg}
    \end{figure}

    \begin{center}
        \hrulefill
    \end{center}

    \begin{center}
        \vspace{1cm}
        \huge\textbf{Exposé}\\[2.5em]
        \normalsize
            in the {{DEGREE_PROGRAM}}\\
            Faculty of Computer Science\\[7em]
        \normalsize
            Thesis Title\\[1em]
        \Large\textbf{{{TITLE}}}
    \end{center}

    \vfill

    \begin{tabular}{ll}
        First and Last Name: & \textbf{{{AUTHOR}}}        \\[1em]
        Student ID:          & {{STUDENT_ID}}             \\[3em]
        First Supervisor:    & {{SUPERVISOR}}             \\[1em]
        Second Supervisor:   & {{SECOND_SUPERVISOR}}      \\[1em]
        Submission Date:     & {{SUBMISSION_DATE}}        \\
    \end{tabular}

\end{titlepage}

\restoregeometry

% ─────────────────────────────────────────────────────────
%  FRONT MATTER
% ─────────────────────────────────────────────────────────
\pagenumbering{Roman}
\thispagestyle{myPageStyle}
\tableofcontents
\cleardoublepage

% ─────────────────────────────────────────────────────────
%  MAIN CONTENT
% ─────────────────────────────────────────────────────────
\pagenumbering{arabic}
\thispagestyle{myPageStyle}

{{BODY}}

% ─────────────────────────────────────────────────────────
%  BACK MATTER
% ─────────────────────────────────────────────────────────
\pagenumbering{Roman}
\setcounter{page}{6}

% ── List of Abbreviations (empty when no abbreviations were declared) ──
{{GLOSSARY_PRINT}}

% ── Bibliography ─────────────────────────────────────────
\thispagestyle{myPageStyle}
\bibliographystyle{dinat}
\renewcommand{\refname}{Bibliography}
\bibliography{literature}
\cleardoublepage

\shorthandon{"}

\end{document}
