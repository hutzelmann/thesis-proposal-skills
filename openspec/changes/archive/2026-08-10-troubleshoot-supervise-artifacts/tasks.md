# Tasks: troubleshoot-supervise-artifacts

## 1. Collector

- [x] 1.1 Add `sibling_artifacts()` to `collect.py` (review file + send-package, hash-level, placeholder names, no content at any level) and wire it into the report's Proposal section; unit tests for supervise workspace, student workspace, and full-level exclusion.

## 2. Skill wording

- [x] 2.1 Generalize the rung-2 sentence so the override wins for student and supervisor users alike.

## 3. Verification

- [x] 3.1 `uv run poe test` and `uv run poe specs` green.
