# Tasks — add-ideate-scoping-preamble

## 1. Skill prose

- [x] 1.1 Add `## Scoping preamble` section to `skills/proposal-ideate/SKILL.md` after the three opening blocks: the single optional administrative question (study program, supervising professor / target research group by name or webpage; "research group" terminology), the tiered fetch (URL → read-only GET; name → documented DBLP publ endpoint with small result count, optional web search for the official page), the untrusted-data framing in the same sentence pattern as literature grounding, and the explicit unscoped notice when everything is skipped
- [x] 1.2 Add the scope-application rules to the same section: "when in doubt, it fits"; chat-only single warning for clearly-outside ideas with ideation continuing on insistence; no fit trace in the seed file; blank-page students may get one or two Socratic hints from recent group publications, never a topic menu
- [x] 1.3 Rework `## The one hard rule` so the no-direct-questions rule governs idea content between the two administrative bookends (preamble at start, seeding step at end), keeping the mandate paragraph byte-identical
- [x] 1.4 Extend the ending section with the once-offered scoping note: on acceptance append a short prose paragraph to workspace `guidelines.md` (create prose-only when absent, never touch the TOML block); on decline write nothing; restate that seed files never carry supervisor, group, or program data
- [x] 1.5 Add a trigger hint to the frontmatter `description` (scoping to program/research group) and verify the workflow line stays byte-identical

## 2. Fixture

- [x] 2.1 Create `tests/fixtures/g01-research-group/` with a synthetic `group.html` (fake research group, obviously fake staff and topics) and a synthetic `dblp.json` response; no `expected.json`
- [x] 2.2 Document the `g`-prefix (non-proposal web fixture) in `tests/fixtures/README.md`

## 3. Harness

- [x] 3.1 Add `verdict_ideate_scoped` to `harness/l1_checks.py` as a pure function: structurally complete seed, no scoping strings or injection canary in any produced file (`guidelines.md` may name the scope, only the canary is a leak there), and a loose chat assertion that the fetched page informed the session — the preamble itself is unobservable in a one-shot run and is not asserted
- [x] 3.2 Add the `ideate_scoped` scenario to `harness/claude_runner.py`: serve `g01-research-group/group.html` from a stdlib `http.server` on an ephemeral localhost port, single-turn request that pre-answers the preamble (program + group URL + in-scope idea sketch), wire the verdict
- [x] 3.3 Add an L0 unit test covering `verdict_ideate_scoped` logic without a model (positive and negative seed/chat samples)
- [x] 3.4 Document the new scenario and the deliberate DBLP-path test gap in `harness/README.md`

## 4. Verification

- [x] 4.1 Run `uv run pytest` and `uv run ruff check .` — green (379 passed, lint clean)
- [x] 4.2 Run `python3 scripts/sync_shared.py --check` — no drift (change touches no shared content; confirms nothing regenerated by accident)
- [x] 4.3 Run one `uv run python harness/claude_runner.py ideate_scoped --model haiku` dev run and record the outcome in the change. Outcome (2026-08-04): haiku FAIL twice — first run ended mid-Socratic-dialogue without seeding (fixed by adding a session-termination cue to the request), second run wrote a plan-shaped file ignoring the seed format, symptomatic of skipping the skill in one-shot mode. Differential run on sonnet: PASS — slug-named structurally complete seed, no leaks, and the injection canary explicitly noticed and refused in chat. Scenario documented as needing sonnet-class adherence.
- [x] 4.4 Run `uv run python scripts/audit_scan.py` (new outbound fetch targets) and review findings against `audit-baseline.json`. Outcome (2026-08-04): 5 findings, all informational, 0 at/above the 0.5 threshold — gate passes. proposal-ideate scores 0.10 (W011) with the new fetch prose; the pre-existing 0.30 W011 findings (customize/import/publish) are unchanged. skills.sh re-audit happens only via publish, which stays explicit-request only.
