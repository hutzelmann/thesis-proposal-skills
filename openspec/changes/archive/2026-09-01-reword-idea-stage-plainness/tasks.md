# Tasks

## 1. Reword the instruction

- [x] 1.1 In `skills/proposal-supervise/SKILL.md`, Curate-the-feedback item 1, replace "say plainly that a proposal cannot be built from the material as it stands" with a clause that states the communicative goal (the student comes away knowing revision alone will not get there), requires the statement in the feedback's own words tied to what this submission is missing, and forbids echoing the instruction sentence or referring to the standard's elements by count.

## 2. Verify

- [x] 2.1 Confirm the tier-phrase requirement and the closing-note verbatim rule in the same list are untouched.
- [x] 2.2 Run `uv run poe test` — header-pattern, mandate-pin, and drift gates must stay green (the reworded clause is outside all pinned regions).
