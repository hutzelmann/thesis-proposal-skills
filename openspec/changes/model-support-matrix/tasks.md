# Tasks: model-support-matrix

## 1. Registry and pure logic

- [x] 1.1 Write `harness/models.toml`: nine pinned models (haiku-4.5, sonnet-5, opus-5, gpt-5.6-luna, gpt-5.6-terra, gpt-5.6-sol, deepseek-v4-pro, qwen3.8-max, kimi-k3) with family/tier/pricing/enabled; `[tasks]` table with core, heavy, excluded_l1, excluded sets and token priors
- [x] 1.2 Write `harness/support.py` (pure, stdlib-only): registry parsing, epoch plan, cell classification, per-model verdict, cost estimate, actual-cost from usage, README marker splice, summary/grid rendering
- [x] 1.3 L0 tests `tests/unit/test_support_matrix.py`: registry happy/edge cases, band edges (all-pass/mixed/all-fail/excluded/reduced-epoch), verdict naming, estimate arithmetic, splice idempotence + untested-model row

## 2. Runners

- [x] 2.1 Write `harness/matrix.py`: argparse (`--tier --models --tasks --epochs --yes --log-dir`), roster/task resolution via support.py, pre-run estimate + confirmation gate, `inspect_ai.eval()` invocation with epochs and token_limit, post-run actual-cost print + `logs/evals/matrix-usage.json` persistence
- [x] 2.2 Write `harness/report.py`: newest-log selection via `inspect_ai.log`, classification via support.py, README region splice + `docs/model-support.md` grid write
- [x] 2.3 Register poe tasks `smoke`, `matrix`, `report`; verify `--help` passthrough for each

## 3. Docs

- [x] 3.1 README.md: add generated-marker section (initial "untested" state generated by report.py, not hand-written)
- [x] 3.2 harness/README.md + AGENTS.md: matrix/smoke/report commands, cost gate behavior, classification semantics, exclusion rationale

## 4. Validation (metered, cost-gated)

- [x] 4.1 `uv run poe test` green; ruff clean; `openspec validate --all --strict` green
- [x] 4.2 Smoke run (1 cheap model × core tasks × 1 epoch) after operator cost approval; verify logs, actual-cost output, usage persistence
- [ ] 4.3 Full matrix run after operator cost approval; `poe report`; review README + grid; record findings here

Validation findings so far (2026-08-06): adversarial review confirmed and fixed four defects (per-run vs per-epoch history, unqualified "supported" on partial coverage, task ids instead of skill names, disabled models dropped from report). Probes ($1.03): haiku flaky on write/customize (both passed 2026-07/08, failed today — genuine flakiness, matrix will quantify); deepseek-v4-pro and kimi-k3 pass write_from_seed clean; gpt-5.6-luna blocked by Azure strict schema validation of Inspect's text_editor tool (model handles tools fine via direct probe; account data policy pins luna to Azure) — kept in the matrix, cells fail honestly at ~$0. Cache-read billing added with catalog rates. Plan B (8 tasks, epochs 3/2/1) re-priced at $77.27; full run deferred at the operator gate — start it later with `uv run poe matrix`.
