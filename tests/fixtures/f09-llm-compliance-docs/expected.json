{
  "fixture": "f09-llm-compliance-docs",
  "lang": "en",
  "check": {
    "exit_code": 1,
    "errors_contain": [
      "required section missing: `Objectives`",
      "required section missing: `Related Work`",
      "required section missing: `Expected Contributions and Results`",
      "required section missing: `Work Plan and Schedule`",
      "only 3 references — at least 10 required"
    ],
    "warnings_contain": [
      "metadata block has no `title:`",
      "reference `Beck23Compliance` defined but never cited",
      "reference `Iyer25Prompting` defined but never cited",
      "reference `Novak24Auditing` defined but never cited",
      "three consecutive sentences start with `Which`"
    ]
  },
  "semantic": [
    "All three research questions are phrased as bare 'Which ...' questions without analytical degree phrasing (to what degree, under which conditions)",
    "Undeclared mixed methods: a prototype extraction pipeline is implemented inside the Systematic Literature Review instead of declaring the Mixed Methods branch",
    "Author metadata is missing (import artifact; check.py only flags the missing title)"
  ]
}
